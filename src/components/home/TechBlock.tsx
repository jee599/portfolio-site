// Intentionally empty after redesign. Live metrics and architecture
// diagrams lived here; those were placeholder-heavy and pulled focus
// from the projects grid. Removed to keep the page type-led.
// The component is preserved so `src/pages/index.astro` (untouched)
// keeps compiling without changes to its import list.
export default function TechBlock() {
  return null;
}
